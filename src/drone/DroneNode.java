package drone;

/**
 * Represents the depot node where the drone starts and returns for battery replacement.
 */
public class DroneNode extends SpatialNode {
    public DroneNode(double x, double y, double z) {
        super(x, y, z);
    }
}